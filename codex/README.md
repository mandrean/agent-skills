# Codex Skills

Store Codex skills here.

Create one subdirectory per skill under `codex/skills/`. Each Codex skill should include a `SKILL.md` file and may include `agents/`, `references/`, `scripts/`, and `assets/`.

## Current Skills

- `gh-cli`: Use `gh` for GitHub repository, PR, issue, Actions, and `gh api` workflows
